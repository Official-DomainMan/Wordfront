{
  "name": "wordfront",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix client ci && npm --prefix client run build && npm --prefix server install",
    "start": "npm --prefix server start"
  },
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  }
}
